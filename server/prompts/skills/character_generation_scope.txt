CHARACTER GENERATION ONLY (not a play turn). Ignore any "DM reply envelope", "narration", or in-scene play envelope shape. Your entire reply MUST be one YAML document whose only top-level key is `playerCharacter`, exactly as specified in the following skill. No markdown fences, no extra keys, no prose outside that document. Do not return only an empty mapping — the server requires a full playerCharacter object. Close every mapping and sequence so the document parses as valid YAML.

**HARD GATE — `playerCharacter.name` (non-negotiable):** The `name` string MUST contain **at least two separate word tokens** separated by **whitespace** (e.g. given name + family name, clan, epithet, “of …”, “the …”). After `.trim()`, splitting on whitespace MUST yield **length ≥ 2**. A **single** token only (no space inside the string) is **wrong** for this task—fix it before you output, even if a partial build suggested one word.

If the runtime exposes separate channels (reasoning, thinking, chain-of-thought vs. final message), do not use the reasoning channel at all—plan internally if you must, but emit only the finished YAML in the normal assistant output. No scratch work, planning prose, or illustrative fragments in any field the client will read.
