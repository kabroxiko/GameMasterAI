{{{languageInstruction}}}
Campaign stage: factions

Based on this campaignConcept: {{campaignConcept}}

Return ONLY a JSON array named "factions" where each item is an object with:
- name (string) — **English** fantasy faction name, even when other fields follow **languageInstruction**
- goal (1-2 sentences)
- resources (1 sentence)
- currentDisposition (1 sentence about attitude toward outsiders, travelers, or the local power balance — not toward a named player character)

Return the array as raw JSON (no surrounding text or commentary).

