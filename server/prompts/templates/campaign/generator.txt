## Campaign metadata builder (not an in-play DM turn)
Campaign metadata builder (this is NOT an in-play Dungeon Master turn):
- Output only the **YAML** structure the user message specifies: either the campaign core mapping (title, campaignConcept, majorConflicts, toneAndStyle, dmHiddenAdventureObjective) or, for a stage request, the value that message describes (list or mapping).
- Do NOT use the live-play DM reply envelope: no top-level narration, imminentCombat, combatCue, or encounter blocks unless the user message explicitly asks for them (it will not).
- Do NOT wrap the document in markdown code fences and do not add prose before or after the YAML.

**Creative seed (when the block below is non-empty):** The server ran a **prior** model pass that produced a **one-off** steering object for **this** campaign only. That data (JSON in the template) is **authoritative** for this request:
- **titleMood** sets emotional **register** for the player-facing **title** only; the title’s **words** must still **name or pin the specific premise** you put in `campaignConcept` and `majorConflicts` (not a generic mood phrase disconnected from those fields).
- Let **preferAngles** visibly shape `campaignConcept`, `majorConflicts`, `toneAndStyle`, and `dmHiddenAdventureObjective`.
- Treat **avoidRepeatedFantasyTropesThisRun** as a **hard skip list** for the **title** and the conceptual spine (the model that wrote the list chose different items each generation — follow them).
- Apply **namingNote** when it is present (internal guidance for evocative, non-repetitive naming).
If **creativeSeed** is missing or the block is literally `{}`, still produce a distinctive campaign without that prior draw.

**creativeSeed (JSON):**
{{creativeSeedJson}}

---

Campaign core policy (DM-ONLY):
- **World-only task:** The server does **not** supply player characters, `gameSetup`, or backstory for this request. `sessionSummary` is intentionally empty. Invent **only** setting-scale premise: no PC names, no “the hero who…”, no assumed class/race.
- Produce a concise campaign core only: include "title" (short player-facing campaign name), "campaignConcept" (2 sentences), "majorConflicts" (3-5 short strings), "toneAndStyle" (1-2 adjectives), and "dmHiddenAdventureObjective" (DM-only string). Keep outputs minimal and focused.
- **Title↔spine:** Design `title`, `campaignConcept`, `majorConflicts`, and `dmHiddenAdventureObjective` as **one** coherent package. The **title** must read as the **proper name or epithet of *this* premise**—someone browsing a save list should recognize *which* crisis or power struggle it refers to. **Invalid:** titles that are only interchangeable atmosphere (stock shadow/war/doom wording) with **no** clear link to the conflicts you list. **Valid:** a short hook paraphrased from **who / what / where / which institution or relic** in your own `campaignConcept` or `majorConflicts` (still 2–8 words, not a full sentence).
- Output MUST be valid YAML. Do NOT include any commentary, examples, or narrative beyond the allowed fields.

Session summary (may be empty): {{sessionSummary}}

Host/table premise (optional; may be empty — if non-empty, use as a creative seed only; world-only, no PC names): {{hostPremise}}

Instructions:
- OUTPUT ONLY valid YAML: one mapping (no surrounding markdown or commentary). Example shape (your strings go in place of the ellipses):

title: ...
campaignConcept: |
  ...
majorConflicts:
  - ...
  - ...
toneAndStyle: ...
dmHiddenAdventureObjective: |
  ...

- The mapping MUST contain these top-level keys (exact names required):
  1) `title` (string): a short **fantasy** name (roughly 2–8 words) in the **same language as the session language policy** (system message). It must **summarize this campaign’s own spine**—the same central stakes and threads as your `campaignConcept` and `majorConflicts`—not a generic fantasy slogan that could fit any world. **Spanish sessions:** Spanish; **English sessions:** English. Match **creativeSeed.titleMood** for **tone** only; do **not** sacrifice concrete anchoring to mood. Honor **avoidRepeatedFantasyTropesThisRun** when present. No mundane real-world person/city names; no colon-separated subtitles; no full sentences. Other string fields follow that same session language policy for prose language.
  2) `campaignConcept` (string): a concise, 2-sentence high-level summary of the campaign's core premise and stakes (world-level — politics, religion, factions, crises). Do NOT include narrative prose or an opening scene here.
  3) `majorConflicts` (sequence of short strings): short descriptions of 3–5 ongoing large-scale conflicts or tensions that drive the campaign.
  4) `toneAndStyle` (string): 1–2 short adjectives describing voice and tone. Do NOT provide examples or sample adjectives.
  5) `dmHiddenAdventureObjective` (string): 2–4 sentences, DM-only. Describe the concrete situation or outcome you will secretly steer play toward (who must be found, what secret must surface, what place must be reached, what threat must be stopped, etc.). **Anchor** it to the same **factions, regions, and conflicts** you already put in `majorConflicts`, `toneAndStyle`, and (when present) implied geography—so a first-session scene can **surface** those threads without contradicting this note. Write it as operational DM notes, not player-facing quest text. Do NOT frame it as a labeled quest given to the PC; the player discovers the path through play. Never name the player character. This field must never be spoken aloud to players as exposition.

Constraints:
- The output must be valid YAML only. Do NOT include any explanatory text, examples, or markdown outside the structure.
- Keep fields concise — avoid narrative paragraphs except inside the small fields allowed (briefDescription).
- Do NOT reference or include player character names or backstories in this output. The campaign core and majorConflicts should be usable independently from any specific PC.

Return the single YAML document as the entire response.
